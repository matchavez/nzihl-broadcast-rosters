[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nzihl-broadcast-rosters"
version = "0.1.0"
description = "Auto-generated NZIHL roster sheets for broadcast booth use."
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31",
    "reportlab>=4.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
nzihl_rosters = ["assets/logos/*.png"]
